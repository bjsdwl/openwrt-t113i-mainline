# 1. 目标架构选择 (Target System)
CONFIG_TARGET_sunxi=y
CONFIG_TARGET_sunxi_cortexa7=y

# 2. 宿主设备选择 (Host Device)
# 我们依然选择 OrangePi One 作为编译环境的容器，以便 OpenWrt 正确设置交叉编译工具链
CONFIG_TARGET_sunxi_cortexa7_DEVICE_xunlong_orangepi-one=y

# 3. 关键切换：禁用 H3 的 U-Boot
# 必须显式禁用，否则构建系统会编译两份 U-Boot，或者因为 H3 补丁冲突导致失败
# CONFIG_PACKAGE_uboot-sunxi-orangepi_one is not set

# 4. 关键切换：启用 T113 的 U-Boot
# 这是我们在 diy-part2.sh 中通过修改 Makefile 注册的新 Target
# 对应 configs/t113_nagami_defconfig
CONFIG_PACKAGE_uboot-sunxi-t113_nagami=y
