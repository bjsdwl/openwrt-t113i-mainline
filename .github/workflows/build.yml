name: T113-i Native Build (Standard Cache)

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths: [ 'patches-uboot/**', 'seed.config', 'diy-part2.sh' ]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Free Disk Space & Initialization
        run: |
          # 释放 Actions 磁盘空间 (30GB+)
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          sudo -E apt-get -qq update
          # 补充 binman 所需的主机工具，增加 device-tree-compiler 用于反编译 DTS
          sudo -E apt-get -qq install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool lrzsz mkisofs msmtp ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pyelftools python3-setuptools python3-mako python3-ply qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev u-boot-tools gdisk libgnutls28-dev
          sudo mkdir -p /workdir
          sudo chown $USER:$GROUPS /workdir

      - name: Clone OpenWrt Source
        run: |
          cd /workdir
          git clone --depth 1 https://github.com/openwrt/openwrt.git openwrt
          ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

      - name: Cache Toolchain
        id: cache-toolchain
        uses: actions/cache@v4
        with:
          path: |
            openwrt/staging_dir
            openwrt/build_dir/host
            openwrt/build_dir/toolchain-*
          key: ${{ runner.os }}-openwrt-toolchain-v2-${{ hashFiles('openwrt/include/toplevel.mk') }}
          restore-keys: |
            ${{ runner.os }}-openwrt-toolchain-v2-

      - name: Update & Install Feeds
        run: |
          cd openwrt
          chmod +x $GITHUB_WORKSPACE/diy-part1.sh
          $GITHUB_WORKSPACE/diy-part1.sh
          ./scripts/feeds update -a
          ./scripts/feeds install -a || echo "⚠️ Ignoring some feed errors"

      - name: Apply Custom Configuration
        run: |
          cd openwrt
          chmod +x $GITHUB_WORKSPACE/diy-part2.sh
          $GITHUB_WORKSPACE/diy-part2.sh
          [ -e $GITHUB_WORKSPACE/seed.config ] && cp $GITHUB_WORKSPACE/seed.config .config
          make defconfig

      - name: Compile U-Boot
        id: compile
        run: |
          cd openwrt
          # 仅编译 U-Boot 包，开启 V=s 观察 binman 输出
          make package/boot/uboot-sunxi/compile V=s -j$(nproc)
          echo "status=success" >> $GITHUB_OUTPUT

      - name: Deep Search & Forensics (Diagnostic Mode)
        if: steps.compile.outputs.status == 'success'
        run: |
          mkdir -p $GITHUB_WORKSPACE/outputs
          echo ">>> [CRITICAL] Starting Forensics on U-Boot Artifacts..."

          # 1. 定位编译目录
          UBOOT_BUILD_DIR=$(find openwrt/build_dir -type d -name "u-boot-2025.01" | head -n 1)
          if [ -z "$UBOOT_BUILD_DIR" ]; then
            echo "❌ Error: Could not find U-Boot build directory!"
            exit 1
          fi
          echo ">>> Found Build Dir: $UBOOT_BUILD_DIR"

          # 2. 提取 DTB 并反编译 (诊断目标：查看 binman offset 是否为 0x20000)
          if [ -f "$UBOOT_BUILD_DIR/u-boot.dtb" ]; then
              echo ">>> Processing u-boot.dtb (The Control DTB)..."
              # 备份二进制 DTB
              cp "$UBOOT_BUILD_DIR/u-boot.dtb" "$GITHUB_WORKSPACE/outputs/debug-u-boot.dtb"
              
              # 反编译为 dts 文本，用于检查 binman 节点和 offset
              dtc -I dtb -O dts "$UBOOT_BUILD_DIR/u-boot.dtb" > "$GITHUB_WORKSPACE/outputs/debug-final-u-boot.dts"
          else
              echo "⚠️ Warning: u-boot.dtb not found!"
          fi

          # 3. 提取 SPL 相关文件 (诊断目标：检查 SPL 大小是否溢出 32KB)
          echo ">>> Processing SPL binaries..."
          # debug-raw-spl.bin: 纯代码，无 DTB
          [ -f "$UBOOT_BUILD_DIR/spl/u-boot-spl.bin" ] && cp "$UBOOT_BUILD_DIR/spl/u-boot-spl.bin" "$GITHUB_WORKSPACE/outputs/debug-raw-spl.bin"
          # debug-spl-with-dtb.bin: 代码 + DTB (关键：查看大小)
          [ -f "$UBOOT_BUILD_DIR/spl/u-boot-spl-dtb.bin" ] && cp "$UBOOT_BUILD_DIR/spl/u-boot-spl-dtb.bin" "$GITHUB_WORKSPACE/outputs/debug-spl-with-dtb.bin"
          # debug-sunxi-spl-final.bin: 最终带 eGON 头的 SPL
          [ -f "$UBOOT_BUILD_DIR/sunxi-spl.bin" ] && cp "$UBOOT_BUILD_DIR/sunxi-spl.bin" "$GITHUB_WORKSPACE/outputs/debug-sunxi-spl-final.bin"

          # 4. 提取 Main U-Boot 合体镜像 (原有逻辑)
          find openwrt/ -type f \( \
            -name "u-boot-sunxi-with-spl.bin" -o \
            -name "*-with-spl.bin" -o \
            -name "u-boot.img" \
          \) -exec cp {} $GITHUB_WORKSPACE/outputs/ \; || true

          # 5. 打印文件大小供快速诊断
          echo ">>> [INFO] Artifact Sizes (Check if SPL > 32KB):"
          ls -l $GITHUB_WORKSPACE/outputs/

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        if: steps.compile.outputs.status == 'success'
        with:
          name: T113i-Uboot-Diagnostics
          path: ${{ github.workspace }}/outputs/*
