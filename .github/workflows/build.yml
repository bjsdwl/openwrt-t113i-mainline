name: T113-i U-Boot Build with Cache

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'diy-part1.sh'
      - 'diy-part2.sh'
      - 'seed.config'

env:
  REPO_URL: https://github.com/openwrt/openwrt
  REPO_BRANCH: master
  CONFIG_FILE: seed.config
  DIY_P1_SH: diy-part1.sh
  DIY_P2_SH: diy-part2.sh
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Initialization Environment
        run: |
          sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          sudo -E apt-get -qq update
          sudo -E apt-get -qq install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool lrzsz mkisofs msmtp ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pyelftools python3-setuptools python3-mako python3-ply qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev u-boot-tools gdisk libgnutls28-dev
          sudo mkdir -p /workdir
          sudo chown $USER:$GROUPS /workdir

      - name: Clone Source Code
        run: |
          cd /workdir
          git clone --depth 1 $REPO_URL -b $REPO_BRANCH openwrt
          ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

      - name: Cache Toolchain
        id: cache-toolchain
        uses: actions/cache@v4
        with:
          path: |
            openwrt/staging_dir
            openwrt/build_dir/host
            openwrt/build_dir/toolchain-*
          key: ${{ runner.os }}-openwrt-toolchain-v2-${{ hashFiles('openwrt/include/toplevel.mk') }}
          restore-keys: |
            ${{ runner.os }}-openwrt-toolchain-v2-

      - name: Update & Install Feeds
        run: |
          cd openwrt
          chmod +x $GITHUB_WORKSPACE/$DIY_P1_SH
          $GITHUB_WORKSPACE/$DIY_P1_SH
          ./scripts/feeds update -a
          ./scripts/feeds install -a || echo "⚠️ Non-critical feed errors ignored"
          ./scripts/feeds install uboot-sunxi

      - name: Load Custom Configuration
        run: |
          cd openwrt
          [ -e $GITHUB_WORKSPACE/$CONFIG_FILE ] && cp $GITHUB_WORKSPACE/$CONFIG_FILE .config
          chmod +x $GITHUB_WORKSPACE/$DIY_P2_SH
          $GITHUB_WORKSPACE/$DIY_P2_SH
          make defconfig

      - name: Build Tools & Toolchain
        if: steps.cache-toolchain.outputs.cache-hit != 'true'
        run: |
          cd openwrt
          make tools/install -j$(nproc) || make tools/install V=s
          make toolchain/install -j$(nproc) || make toolchain/install V=s

      - name: Compile U-Boot
        id: compile
        run: |
          cd openwrt
          make package/boot/uboot-sunxi/compile -j$(nproc) V=s
          echo "status=success" >> $GITHUB_OUTPUT

      - name: Process & Sign Binaries (The eGON Fix)
        if: steps.compile.outputs.status == 'success'
        run: |
          mkdir -p $GITHUB_WORKSPACE/outputs
          cd openwrt
          
          # 查找构建目录
          UBOOT_BUILD_DIR=$(find build_dir/target-* -name "u-boot-20*" | grep "/u-boot-sunxi/" | head -n 1)
          echo ">>> Processing from: $UBOOT_BUILD_DIR"
          
          if [ -d "$UBOOT_BUILD_DIR" ]; then
              cd "$UBOOT_BUILD_DIR"
              
              # 1. 提取原始产物
              cp spl/sunxi-spl.bin $GITHUB_WORKSPACE/outputs/sunxi-spl-debug.bin
              cp u-boot.img $GITHUB_WORKSPACE/outputs/u-boot-debug.img
              
              # 2. 【核心】重新计算 Checksum 确保 BROM 加载
              # 算法：累加所有 32bit 字（排除 Checksum 位本身），最后存入 0x0c
              python3 -c "
          import struct, os
          filename = 'spl/sunxi-spl.bin'
          with open(filename, 'rb') as f:
              data = bytearray(f.read())
          
          # 获取实际长度并对齐 4 字节
          length = struct.unpack('<I', data[0x10:0x14])[0]
          length = (length + 3) & ~3
          
          checksum = 0
          for i in range(0, length, 4):
              if i == 0x0c:
                  val = struct.unpack('<I', b'eGON')[0]
              else:
                  val = struct.unpack('<I', data[i:i+4])[0]
              checksum = (checksum + val) & 0xFFFFFFFF
          
          struct.pack_into('<I', data, 0x0c, checksum)
          with open('spl_signed.bin', 'wb') as f:
              f.write(data)
          print(f'✅ Fixed eGON Checksum: {hex(checksum)}')
          "
              
              # 3. 合成最终 raw 镜像
              # 布局：SPL @ 0, U-Boot Proper @ 32KB (0x8000)
              cp spl_signed.bin $GITHUB_WORKSPACE/outputs/t113i-uboot-raw.bin
              truncate -s 32K $GITHUB_WORKSPACE/outputs/t113i-uboot-raw.bin
              cat u-boot.img >> $GITHUB_WORKSPACE/outputs/t113i-uboot-raw.bin
              
              echo "✅ t113i-uboot-raw.bin generated successfully."
          else
              echo "❌ ERROR: Build directory not found!"
              exit 1
          fi

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        if: steps.compile.outputs.status == 'success'
        with:
          name: T113i-Uboot-Final-Signed
          path: ${{ github.workspace }}/outputs/*
