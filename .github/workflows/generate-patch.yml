name: OpenWrt - Step 1 Nagami Base (PC0 LED Only)

on:
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y build-essential git quilt libssl-dev

      - name: Clone OpenWrt & Prepare Source
        run: |
          git clone --depth=1 https://github.com/openwrt/openwrt.git -b master openwrt
          cd openwrt
          sed -i 's/PKG_VERSION:=.*/PKG_VERSION:=2025.01/g' package/boot/uboot-sunxi/Makefile
          sed -i 's/PKG_HASH:=.*/PKG_HASH:=skip/g' package/boot/uboot-sunxi/Makefile
          ./scripts/feeds update -a
          ./scripts/feeds install uboot-sunxi
          echo "CONFIG_TARGET_sunxi=y" > .config
          echo "CONFIG_TARGET_sunxi_cortexa7=y" >> .config
          echo "CONFIG_TARGET_sunxi_cortexa7_DEVICE_xunlong_orangepi-one=y" >> .config
          make defconfig
          make package/boot/uboot-sunxi/prepare V=s

      - name: Generate Step 1 Patch (Violent PC0 LED)
        run: |
          UBOOT_DIR=$(find openwrt/build_dir -type d -name "u-boot-2025.01" | head -n 1)
          cd "$UBOOT_DIR"
          mkdir -p patches
          
          # 只生成一个补丁：暴力点亮 PC0
          quilt new 001-violent-pc0-led.patch
          quilt add arch/arm/mach-sunxi/board.c
          
          python3 -c "
          import sys
          code = [
              '\t{ /* Step 1: Violent PC0 LED On */\n',
              '\t\t*(volatile unsigned int *)0x0200190C = 0x00010001; /* 1. PIO Clock Gate */\n',
              '\t\t*(volatile unsigned int *)0x02000128 = 0x00000070; /* 2. Port G/C/F Power Gate */\n',
              '\t\t*(volatile unsigned int *)0x02000060 = (*(volatile unsigned int *)0x02000060 & ~0xF) | 1; /* 3. PC0 CFG -> Output */\n',
              '\t\t*(volatile unsigned int *)0x02000070 |= 1; /* 4. PC0 DATA -> High */\n',
              '\t}\n'
          ]
          with open('arch/arm/mach-sunxi/board.c', 'r') as f: lines = f.readlines()
          for i, line in enumerate(lines):
              if 'void board_init_f(ulong dummy)' in line:
                  for j in range(i, i+5):
                      if '{' in lines[j]:
                          lines[j+1:j+1] = code
                          break
                  break
          with open('arch/arm/mach-sunxi/board.c', 'w') as f: f.writelines(lines)
          "
          quilt refresh
          
          mkdir -p $GITHUB_WORKSPACE/patches-uboot
          cp patches/00*.patch $GITHUB_WORKSPACE/patches-uboot/

      - name: Sync and Commit
        run: |
          cd $GITHUB_WORKSPACE
          git config --global user.name "actions-user"
          git config --global user.email "actions@github.com"
          git add patches-uboot/*.patch
          git commit -m "fix: step 1 violent pc0 led for nagami base" || echo "No changes"
          git push
