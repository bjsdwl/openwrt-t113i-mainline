name: T113-i Identity Fix Patch Factory

on:
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Tools
        run: sudo apt-get update && sudo apt-get install -y build-essential git quilt libssl-dev device-tree-compiler perl

      - name: Prepare Source Environment
        run: |
          git clone --depth=1 https://github.com/openwrt/openwrt.git -b master openwrt
          cd openwrt
          ./scripts/feeds update -a && ./scripts/feeds install uboot-sunxi
          
          # ⚠️ 关键步骤：在编译前先准备好环境，但不生成 config
          # 我们需要先打补丁，再生成 config，因为我们要新增 Target
          make package/boot/uboot-sunxi/prepare V=s

      - name: "💉 SURGERY: Correct Architecture Definition"
        run: |
          # 1. 定位 OpenWrt 的 Makefile
          OW_MAKEFILE="openwrt/package/boot/uboot-sunxi/Makefile"
          UBOOT_DIR=$(find openwrt/build_dir -name "u-boot-20*" -type d | head -n 1)
          
          if [ -z "$UBOOT_DIR" ]; then echo "❌ U-Boot source not found"; exit 1; fi
          cd "$UBOOT_DIR"
          mkdir -p patches

          # =========================================================
          # Patch 1: 在 OpenWrt Makefile 中注册 T113 (关键！)
          # =========================================================
          # 我们不能在 U-Boot 源码里改 OpenWrt 的 Makefile，
          # 但我们可以生成一个修改 OpenWrt Makefile 的补丁吗？
          # 不行，quilt 只能针对 U-Boot 源码。
          # 所以：我们必须把这个修改作为“非 quilt”操作，
          # 或者，我们在 U-Boot 源码里添加真正的 T113 defconfig。
          
          echo ">>> Creating Patch 001: Add T113 Defconfig & DTS..."
          quilt new 001-add-t113-support.patch
          
          # 1.1 添加 DTS (照搬 Nagami)
          quilt add arch/arm/dts/sun8i-t113s-nc-nagami.dts
          cat << 'EOF' > arch/arm/dts/sun8i-t113s-nc-nagami.dts
          /dts-v1/;
          #include "sun8i-t113s.dtsi"
          #include <dt-bindings/gpio/gpio.h>
          / {
              model = "Tronlong TLT113-MiniEVM (Final)";
              compatible = "tronlong,tlt113-minievm", "allwinner,sun8i-t113i", "allwinner,sun8i-t113s";
              aliases { serial0 = &uart0; mmc0 = &mmc0; };
              chosen { stdout-path = "serial0:115200n8"; };
              reg_vcc3v3: vcc3v3 {
                  compatible = "regulator-fixed";
                  regulator-name = "vcc-3v3";
                  regulator-min-microvolt = <3300000>;
                  regulator-max-microvolt = <3300000>;
                  regulator-always-on;
                  bootph-all;
              };
          };
          &ccu { bootph-all; };
          &pio {
              bootph-all;
              uart0_pg_pins: uart0-pg-pins { pins = "PG17", "PG18"; function = "uart0"; bootph-all; };
              mmc0_pins: mmc0-pins { pins = "PF0", "PF1", "PF2", "PF3", "PF4", "PF5"; function = "sdc0"; drive-strength = <30>; bias-pull-up; bootph-all; };
          };
          &uart0 { pinctrl-names = "default"; pinctrl-0 = <&uart0_pg_pins>; status = "okay"; bootph-all; };
          &mmc0 { pinctrl-names = "default"; pinctrl-0 = <&mmc0_pins>; vmmc-supply = <&reg_vcc3v3>; bus-width = <4>; broken-cd; status = "okay"; bootph-all; };
          EOF
          
          # 1.2 添加 Defconfig (真正的独立文件，不覆盖 H3)
          quilt add configs/t113_nagami_defconfig
          cat << 'EOF' > configs/t113_nagami_defconfig
          CONFIG_ARM=y
          CONFIG_ARCH_SUNXI=y
          CONFIG_DEFAULT_DEVICE_TREE="sun8i-t113s-nc-nagami"
          CONFIG_SPL=y
          CONFIG_MACH_SUN8I_R528=y
          CONFIG_SPL_OF_CONTROL=y
          CONFIG_SPL_OF_PLATDATA=y
          CONFIG_SPL_MAX_SIZE=0x1d000
          CONFIG_SPL_PAD_TO=0x20000
          CONFIG_DRAM_CLK=792
          CONFIG_DRAM_ZQ=8092667
          CONFIG_DRAM_TYPE_DDR3=y
          CONFIG_DRAM_SUNXI_ODT_EN=1
          CONFIG_DRAM_SUNXI_TPR0=0x004A2195
          CONFIG_DRAM_SUNXI_TPR1=0x02423190
          CONFIG_DRAM_SUNXI_TPR2=0x0008B061
          CONFIG_DRAM_SUNXI_TPR3=0xB4787896
          CONFIG_DRAM_SUNXI_TPR4=0x0
          CONFIG_DRAM_SUNXI_TPR5=0x48484848
          CONFIG_DRAM_SUNXI_TPR6=0x00000048
          CONFIG_DRAM_SUNXI_TPR7=0x1620121e
          CONFIG_DRAM_SUNXI_TPR11=0x00770000
          CONFIG_DRAM_SUNXI_TPR12=0x00000002
          CONFIG_DRAM_SUNXI_TPR13=0x34050100
          CONFIG_SPL_STACK_R=y
          CONFIG_SPL_STACK_R_ADDR=0x81e00000
          CONFIG_SYS_MMCSD_RAW_MODE_U_BOOT_USE_SECTOR=y
          CONFIG_SYS_MMCSD_RAW_MODE_U_BOOT_SECTOR=0x110
          CONFIG_SPL_MMC=y
          CONFIG_SPL_SERIAL=y
          CONFIG_DM_SERIAL=y
          CONFIG_DM_MMC=y
          CONFIG_SPL_SYS_MALLOC_SIMPLE=y
          EOF
          quilt refresh

          # =========================================================
          # Patch 2: Board.c 硬件解锁 (Nagami 逻辑)
          # =========================================================
          echo ">>> Creating Patch 002: Hardware Unlock..."
          quilt new 002-board-init-hack.patch
          quilt add arch/arm/mach-sunxi/board.c
          
          python3 -c "
          import sys
          hack_code = [
              '\t{ /* FUSION: Physical Hardware Force Unlock */\n',
              '\t\tvolatile unsigned int *pio = (volatile unsigned int *)0x0200190C;\n',
              '\t\t*pio = 0x00010001; /* Unlock PIO Gate */\n',
              '\t\t*(volatile unsigned int *)0x0200184C = 0x00010001; /* MMC0 Gate */\n',
              '\t\t*(volatile unsigned int *)0x02001830 = 0x80000000; /* MMC0 Clock */\n',
              '\t\t*(volatile unsigned int *)0x02001128 |= 1;         /* UART0 Gate */\n',
              '\t\t/* Port F (MMC) Drive Strength */\n',
              '\t\t*(volatile unsigned int *)0x020000F0 = 0x00222222;\n',
              '\t\t*(volatile unsigned int *)0x02000100 = 0x00333333;\n',
              '\t\t*(volatile unsigned int *)0x02000104 = 0x00000555;\n',
              '\t\t/* Port G (UART) */\n',
              '\t\t*(volatile unsigned int *)0x02000128 = 0x77777777;\n',
              '\t\t*(volatile unsigned int *)0x02000124 = (*(volatile unsigned int *)0x02000124 & ~(0xF << 12)) | (0x1 << 12);\n',
              '\t\t*(volatile unsigned int *)0x02000130 |= (1 << 11);\n',
              '\t\t/* Port C (LED) */\n',
              '\t\t*(volatile unsigned int *)0x02000060 = 1; *(volatile unsigned int *)0x02000070 |= 1;\n',
              '\t\t/* Debug Char 'K' */\n',
              '\t\tvolatile unsigned int *u0 = (volatile unsigned int *)0x02500000;\n',
              '\t\tu0[3]=0x83; u0[0]=13; u0[1]=0; u0[3]=3; u0[0]=0x4B;\n',
              '\t}\n'
          ]
          try:
              with open('arch/arm/mach-sunxi/board.c', 'r') as f: lines = f.readlines()
              for i, line in enumerate(lines):
                  if 'board_init_f' in line and '{' in line:
                      lines[i:i+1] = [line] + hack_code
                      break
                  elif 'board_init_f' in line: # 处理 { 换行的情况
                      lines[i:i+2] = [lines[i], lines[i+1]] + hack_code
                      break
              with open('arch/arm/mach-sunxi/board.c', 'w') as f: f.writelines(lines)
          except Exception: pass
          "
          quilt refresh

      - name: Sync and Commit
        run: |
          # 1. 定位 U-Boot 源码目录
          UBOOT_DIR=$(find openwrt/build_dir -name "u-boot-20*" -type d | head -n 1)
          if [ -z "$UBOOT_DIR" ]; then echo "❌ U-Boot not found"; exit 1; fi

          # 2. 清空并复制
          mkdir -p $GITHUB_WORKSPACE/patches-uboot
          rm -f $GITHUB_WORKSPACE/patches-uboot/*.patch
          cp $UBOOT_DIR/patches/*.patch $GITHUB_WORKSPACE/patches-uboot/
          
          # 3. 提交
          cd $GITHUB_WORKSPACE
          git config --global user.name "actions-user"
          git config --global user.email "actions@github.com"
          git add -A patches-uboot/
          git commit -m "fix: switch to clean t113 target structure" || echo "No changes"
          git push
