name: 01 - Patch Lint & Validator (Real Source Mode)

on:
  push:
    paths:
      - 'patches-uboot/**'
      - '.github/workflows/check-patches.yml'
  workflow_dispatch:

jobs:
  real_source_check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout User Patches
        uses: actions/checkout@v4

      - name: Install Quilt & Utils
        run: |
          sudo apt-get update
          sudo apt-get install -y quilt dos2unix

      - name: 1. Strict Syntax & EOL Check
        run: |
          echo ">>> Checking for Windows CRLF and Missing Newlines..."
          cd patches-uboot
          # 如果目录为空（虽然不可能，因为有001/002），防止报错
          shopt -s nullglob
          for patchfile in *.patch; do
            echo "Checking $patchfile..."
            if grep -U $'\r' "$patchfile" > /dev/null; then
              echo "❌ ERROR: Windows CRLF found in $patchfile"
              exit 1
            fi
            if [ "$(tail -c 1 "$patchfile" | xxd -p)" != "0a" ]; then
               echo "❌ ERROR: No newline at EOF in $patchfile"
               exit 1
            fi
          done
          echo "✅ Patch formatting is clean."

      - name: 2. Download Real U-Boot v2025.01
        run: |
          echo ">>> Cloning U-Boot v2025.01 (Shallow)..."
          git clone --depth 1 https://github.com/u-boot/u-boot.git -b v2025.01 u-boot-src

      - name: 3. Quilt Apply Test (Real Source)
        run: |
          cd u-boot-src
          mkdir patches
          cp ../patches-uboot/*.patch patches/
          
          # 清洗逻辑
          sed -i 's/^ \+spl_init();/\tspl_init();/' patches/003-early-debug-led.patch
          sed -i 's/^ \+gpio_init();/\tgpio_init();/' patches/003-early-debug-led.patch
          sed -i 's/^ \+preloader_console_init();/\tpreloader_console_init();/' patches/003-early-debug-led.patch
          
          ls patches/*.patch | xargs -n 1 basename > patches/series
          
          if quilt push -a --fuzz=0; then
            echo "🎉 SUCCESS!"
          else
            echo "💥 FAILED!"
            exit 1
          fi
          
          # 生成 patch 列表
          ls patches/*.patch | xargs -n 1 basename > patches/series
          
          echo ">>> Attempting to apply patches to REAL v2025.01 source..."
          # 执行严格校验
          if quilt push -a --fuzz=0; then
            echo "🎉 SUCCESS: All existing patches applied perfectly!"
          else
            echo "💥 FAILED: Patch application failed."
            exit 1
          fi
