Index: u-boot-src/common/spl/spl_mmc.c
===================================================================
--- u-boot-src.orig/common/spl/spl_mmc.c
+++ u-boot-src/common/spl/spl_mmc.c
@@ -1,6 +1,16 @@
 // SPDX-License-Identifier: GPL-2.0+
 /*
  * (C) Copyright 2010
+
+	/* --- DIAG START --- */
+	if (mmc) {
+		static char b[512] __aligned(64);
+		printf("DIAG: Testing MMC%d Sector 16...\n", spl_boot_device_mmc_index(bootdev));
+		if (blk_dread(mmc_get_blk_desc(mmc), 16, 1, b)) {
+			printf("DIAG: Sec16 Magic at 0x04: [%.4s]\n", &b[4]);
+		} else { printf("DIAG: blk_dread failed!\n"); }
+	}
+	/* --- DIAG END --- */
  * Texas Instruments, <www.ti.com>
  *
  * Aneesh V <aneesh@ti.com>
